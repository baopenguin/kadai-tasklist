<% if users.any? %>
  <ul>
    <% users.each do |user| %>
      <li>
        <img src="<%= gravatar_url(user, {size:50}) %>" alt="">
        <%= user.name %>
        <%= link_to 'View profile', user_path(user) %>
      </li>
    <% end %>
  </ul>
  <%= paginate users %>
<% end %>